
#include <iostream>
#include<math.h>
using namespace std;

int main() {
    int num,res=0,i=0;
    cin>>num;
    while(num!=0){
        if(num%10!=0){
        res=res+pow(2,i);
        }
        num=num/10;
        i++;
    }
    cout<<res;
    return 0;
}
